version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: signalstream-qdrant
    ports:
      - "6333:6333" # API port
      - "6334:6334" # gRPC port
    volumes:
      - ./qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage: